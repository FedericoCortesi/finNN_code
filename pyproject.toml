[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "finnn_code"
version = "0.1.0"
description = "Finance utilities and experiments"
authors = [
    { name = "Federico Cortesi" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["utils"]

[tool.setuptools.packages.find]
packages = ["utils*"]